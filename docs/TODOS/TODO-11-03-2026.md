# Demo Polish: Dependency Graph & Prioritized Todo

**As of:** 2026-03-11

**Gate (until workshop is over):** Do not start DESIGN-CORE-03 kernel work or broad refactors until (1) one demo corridor runs end-to-end, (2) card rebuild and hot-reload on that path are stable, and (3) the workshop is done or explicitly deprioritized. If something hurts, fix it on the demo path only and add a note to the fast-path checklist; do not slip into “new kernel” or “elegant topology” work.

---

## 1. Workshop-fast path (next ~3 weeks) — living command center

*Until the workshop is over, this section is the only plan that really matters. The phased roadmap below is for after.*

**Must do:**
- **A1** GC / disposal boundary — propagator-only dispose; scheduler disposes cells when unreachable.
- **A2** Card semantic state: contributions, contradiction, and convergence policy. Expose contradiction/ambiguity at card interface; support multiple active candidate contributions; local and global policy selection; optionally compute all candidates; treat card as specialized compound propagator; on connect/detach rebuild new network and dispose old; preserve enough contribution identity for the demo corridor. *This is now more important than generic hot-reload elegance.*
- **B1** Card as compound propagator; rebuild on connect/detach — on connect, detach, or rebuild: build new network, dispose old (retract premises, GC). No in-place fix.
- **B2** Only the narrow hot-reload path you need — premise/dependency-driven successor networks for the one corridor you demo (TemporaryValueSet + PremisesSource; no need to fix all of incremental_compiler).
- **Protocol simplification only on the demo path** — simplify decoder layer (pattern matcher + generic operators) only where it blocks the demo.
- **Minimal persistence** — loading/saving demo cards so the workshop can “save and reload”; no general storage layer yet.
- **D1** Topology tracking — core for demonstrating multi-projection; needed so the demo can show the graph / projection structure.
- **D2** Metadata/annotation layer — necessary for composable visualization; interested entities + annotation layer so viz can be composed and modified.

**Should do only if time remains:**
- **C3** Semantic card model — only enough to keep the code coherent (no full propagator-like card yet).
- **E1** Syntax — only if the demo absolutely needs missing syntax; if so, pull it into the fast path (see Phase E note below).

**Do not do before workshop unless something explodes:**
- **C4** Full card-as-construct composition
- **E2** Extra reactive operators
- **E3** CORE-03 beyond very light notes
- Reflective card/UI ambitions (network-card vs execution-card split, meta UI)

**Fastest path (operational):**
1. **Stabilize one demo corridor** — one flow that runs end-to-end.
2. **Then** reduce friction on that corridor — rebuild, hot reload, protocol, persistence only where it hurts.
3. **Then** add only the visual or semantic pieces needed to make that corridor impressive.

**Next-weeks plan — four milestones:** *(to be filled)*

---

## 2. Dependency Graph (Conceptual)

```
                    ┌─────────────────────────────────────────────────────────┐
                    │ (5) DESIGN-CORE-03 kernel experiment (20% time)           │
                    │     — graph-based situation calculus                     │
                    └─────────────────────────────────────────────────────────┘
                                              │ informs long-term
                                              ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ FOUNDATION (fix lifecycle & time)                                                                │
│                                                                                                 │
│  (1) GC: propagator-only dispose, scheduler auto-disposes cells when unreachable                │
│       [Propogator scheduler, Cell/Propagator]                                                   │
│                                                                                                 │
│  (7) A2 Card semantic state: contributions, contradiction, convergence policy. Expose contradic-  │
│      tion/ambiguity; multiple candidates; local/global policy; card as compound propagator;     │
│      connect/detach → build new, dispose old; preserve contribution identity. > hot-reload elegance│
│      [card_api.ts, runtime.ts, PremisesSource]                                                   │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
                    │                           │
                    │ unblocks                   │ unblocks
                    ▼                           ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ REPLACEMENT AND REBUILD                                                                         │
│                                                                                                 │
│  (3) Card as specialized compound propagator (card_api / runtime level)                         │
│      — Treat card as compound propagator; on connect/detach (and rebuild): build new network,   │
│        dispose old one (no in-place timestamp fix). Retract premises of old network, GC.       │
│      [lain-lang/src/grpc/card/card_api.ts, runtime.ts, schema.ts]                               │
│                                                                                                 │
│  (2) Hot reload: network expression + card                                                      │
│      — incremental_compiler.ts not functioning; alternative: use dependence tracking           │
│        (TemporaryValueSet, PremisesSource kick_out/bring_in) for premises of unfolded compound   │
│        networks → build new network, gradually dispose when premises retracted                  │
│      [lain-lang/compiler/incremental_compiler.ts, Propogator/DataTypes/TemporaryValueSet.ts,   │
│       Propogator/DataTypes/PremisesSource, Propogator/test/advanceReactive.test.ts,            │
│       Propogator/test/temporaryValueSetIntegration.test.ts, lain-lang/test/compiler.test.ts]     │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
                    │
                    │ unblocks
                    ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ CARD SEMANTICS & VISUALIZATION                                                                   │
│                                                                                                 │
│  (6) Card as construct: pass card/cell construct into card neighbor, spawn sub-cell              │
│      [lain-viz/docs/design-principles.md, lain-lang/src/grpc/card/schema.ts, get_local_env]     │
│                                                                                                 │
│  (4) Topology tracker:                                                                          │
│      4.2 — Metadata layer (interested cell/propagator data) + annotation layer (visualization   │
│            modifiers)                                                                           │
│      4.1 — More efficient algorithm (progressive graph building, not full BFS per update)        │
│      [lain-lang/compiler/primitive/stdlib.ts, compiler/tracer/tracer.ts, graph_queries.ts]       │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
                    │
                    │ feeds into
                    ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ LANGUAGE & REACTIVE PRIMITIVES (can progress in parallel where noted)                           │
│                                                                                                 │
│  (8) Syntax design (lain-lang): branching (when/if), sugar, tail-recursion, compound data,     │
│      premises in core; primitive types (boolean 4-value, interval float, string, array, map, set)│
│      [compiler, primitive_env, parser]                                                          │
│                                                                                                 │
│  (9) Reactive operators: reactive_map, reactive_filter, reactive_reduce, last/index, zip,      │
│      combineLatest; smarter cell (memory for past temp info when neighbor interested)           │
│      [Propogator BuiltInProps, advanceReactive.test.ts; DESIGN-CORE-03 “interest”]               │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
                    │
                    │ unblocks
                    ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ UNIFIED CARD MODEL & META-REFLECTIVE UI                                                          │
│                                                                                                 │
│  (10) Network card vs execution card: network expression → network card; strict separation;   │
│       unified input API; card system multi-projectional; spawned card button as projected card  │
│       [lain-lang card_api, lain-viz card UI]                                                    │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. Prioritized Todo (Phased)

### Phase A — Foundation (do first)
**Target:** Q2 2026

| # | Item | Description | Key files / areas |
|---|------|-------------|-------------------|
| **A1** | **GC: propagator-only dispose** | Expose only propagator `dispose`; scheduler automatically disposes cells when no longer reachable via any propagator. | Propogator: scheduler, Cell/Propagator lifecycle; `runtime.ts` currently uses both `dispose_cell` and `dispose_propagator`. |
DONE

| **A2** | **Card semantic state: contributions, contradiction, and convergence policy** | (1) Expose contradiction/ambiguity at card interface. (2) Support multiple active candidate contributions. (3) Local and global policy selection. (4) Optionally compute all candidates. (5) Treat card as specialized compound propagator; on connect/detach rebuild new network and dispose old. (6) Preserve enough contribution identity for the demo corridor. *More important than generic hot-reload elegance.* | `card_api.ts`, `runtime.ts`, card interface (contradiction + policy + candidates), PremisesSource, contribution identity for demo. |

### Phase B — Replacement and rebuild
**Target:** Q2 2026

| # | Item | Description | Key files / areas |
|---|------|-------------|-------------------|
| **B1** | **Card as compound propagator; rebuild on connect/detach** | Treat card as specialized compound propagator. On connect, detach, or rebuild: build new network (in neighbor cell), dispose old network (retract premises, let GC clean up). No in-place timestamp fix; lifecycle is build-new-then-dispose-old. | `card_api.ts` (connect/detach/build entry points), `runtime.ts` (`runtime_build_card`, `runtime_connect_cards`, `runtime_detach_cards`, `dispose_card_internal_network_io`), `compile_internal_network`, `schema.ts`. |
| **B2** | **Hot reload through premise/dependency-driven successor networks** | Track premises of each unfolded compound network (as in compiler.test.ts); build new network, gradually dispose when premises retracted (TemporaryValueSet + PremisesSource). Fix or replace `incremental_compiler.ts` with this approach. | `incremental_compiler.ts`, `TemporaryValueSet.ts`, `PremisesSource`, `advanceReactive.test.ts`, `temporaryValueSetIntegration.test.ts`, `compiler.test.ts`. |

### Phase C — Card model and persistence
**Target:** Q3 2026  
**Priority:** Architecture refactor (see §4). Focus on checklist items 4–7; C1 is explicitly the decoder/protocol pain point.

| # | Item | Description | Key files / areas |
|---|------|-------------|-------------------|
| **C1** | **Frontend/backend protocol simplification on the hot path** | Simplify decoder layer using pattern matcher + generic operators; scope only to the demo path where it causes friction. | Decoder/session layer, codec; `lain-lang/src/grpc/` (or wherever decoder lives). |
| **C2** | **Storage layer for persisted card content** | Persist card content so it survives reload; define where and how card state is stored. | Card runtime, storage; `card_api.ts`, `runtime.ts`, schema; possible DB or session layer. |
| **C3** | **Semantic card model** | Card as first-class compositional unit, possibly propagator-like; only enough to keep the code coherent. | `design-principles.md`, `schema.ts`, card connector / composition; design docs. |
| **C4** | **Card-as-construct composition** | Pass card as construct; spawn sub-cells and pass cell construct into card neighbor (design-principles.md). | `schema.ts`, `get_local_env`, slots, `internal_cell_*`, `compile_internal_network`. |

### Phase D — Topology and introspection
**Target:** Q3 2026  
**Required for demo:** Topology tracking (D1) is the core for demonstrating multi-projection; metadata/annotation layer (D2) is necessary for composable visualization. Both are on the workshop-fast path (§1).

| # | Item | Description | Key files / areas |
|---|------|-------------|-------------------|
| **D1** | **Efficient progressive graph tracking** | Build graph progressively instead of full upstream BFS on every update. Core for multi-projection demo. | `stdlib.ts` (graph:trace, graph:active-trace), `tracer/tracer.ts` (`trace_upstream_reactively`, `trace_upstream_periodically`). |
| **D2** | **Metadata and annotation layer for interested entities** | Metadata layer for interested cell/propagator data; annotation layer to control how that information is visualized (modifiers). Necessary for composable visualization. | `tracer/`, `graph_queries.ts`, naming/labels; frontend that consumes graph. |

### Phase E — Language surface and reactive primitives
**Target:** Q4 2026  
**Note:** If syntax or operators are actually needed for the workshop demo, pull them into the fast path (§1); do not leave them “politely” in Q4. E1/E2 here are for post-workshop roadmap; only missing syntax required by the demo may be pulled forward.

| # | Item | Description | Key files / areas |
|---|------|-------------|-------------------|
| **E1** | **Syntax needed by demo** | Core syntax required for the demo: branching (when/if), sugar, tail-recursion, compound data, premises in core; primitive types as needed. *Only add what the demo actually needs; pull forward into §1 if workshop-blocking.* | Compiler entry, parser, `primitive_env`, stdlib. |
| **E2** | **Reactive operators needed by demo** | reactive_map, reactive_filter, reactive_reduce, last/index, zip, combineLatest; smarter cell when neighbor interested. | Propogator BuiltInProps, advanceReactive tests. |
| **E3** | **CORE-03 experiments kept capped** | Graph-based situation calculus kernel — cap time (e.g. 20%); keep polishing demo with old kernel. | `lain-viz/docs/DESIGN-CORE-03.md`. |

---

## 4. Architecture refactor (checklist)

Ordered list of architecture refactor items (aligned with phases A–E). **Prioritized in Phase C:** C1–C4 (protocol on hot path, storage, semantic card model, card-as-construct).

1. **GC and A2 card semantic state** — propagator-only dispose; scheduler-driven cell GC. **A2:** contributions, contradiction, convergence policy: expose contradiction/ambiguity at card interface; multiple candidate contributions; local/global policy selection; optionally compute all candidates; card as compound propagator; connect/detach → build new, dispose old; preserve contribution identity for demo. *Prioritized over generic hot-reload elegance.*
2. **Card rebuild protocol** — card as compound propagator; on connect, detach, or rebuild: build new network, dispose old (retract premises, GC). No in-place timestamp fix.
3. **Hot reload / replacement semantics** — premise/dependency-driven successor networks; TemporaryValueSet + PremisesSource.
4. **Frontend/backend protocol simplification on the hot path** — simplify decoder layer using pattern matcher + generic operators; scope to demo path.
5. **Storage layer for persisted card content** — where and how card state is stored across reload.
6. **Semantic card model** — card as compositional unit, possibly propagator-like.
7. **Card-as-construct composition** — spawning and local composition; pass card/cell construct into neighbor.
8. **Topology efficiency** — progressive graph tracking (no full BFS per update).
9. **Topology metadata/annotation** — interested entities; annotation layer for visualization modifiers.
10. **Minimal syntax and reactive operators for demo** — branching, sugar, reactive_map/filter/reduce, zip, etc., as needed.
11. **Reflective network-card / execution-card split and meta UI** — strict separation; unified input API; multi-projectional card system; spawned card as projected card.

---


## 5. Dependency Summary

- **A1 (GC)** and **A2 (card semantic state: contributions, contradiction, convergence policy)** are foundational: safe gradual disposal; card interface exposes contradiction/ambiguity, multiple candidates, local/global policy; connect/detach rebuilds network; contribution identity for demo. A2 is more important than generic hot-reload elegance.
- **B1 (card rebuild without immediate destruction)** and **B2 (hot reload through premise/dependency-driven successor networks)** depend on A1/A2 and directly improve demo behavior.
- **C1–C4** (card model and persistence) cover protocol simplification on hot path, storage, semantic card model, and card-as-construct composition.
- **D1–D2** (topology and introspection) are required for the demo: topology tracking is core for multi-projection; metadata/annotation layer is necessary for composable visualization.
- **E1–E3** (language surface and reactive primitives) cover syntax and reactive operators needed by the demo, with CORE-03 experiments kept capped (E3).

---

## 6. Quick Reference: Where Things Live

| Topic | Location |
|-------|----------|
| Incremental compiler | `lain-lang/compiler/incremental_compiler.ts` |
| Compound network / closure tests | `lain-lang/test/compiler.test.ts` |
| PremisesSource, kick_out, bring_in | `Propogator/DataTypes/PremisesSource`, `advanceReactive.test.ts` |
| TemporaryValueSet, merge | `Propogator/DataTypes/TemporaryValueSet.ts`, `temporaryValueSetIntegration.test.ts` |
| Card runtime, build, dispose; card as compound propagator (connect/detach → build new, dispose old) | `lain-lang/src/grpc/card/runtime.ts`, `schema.ts`, `card_api.ts` |
| Graph tracer | `lain-lang/compiler/tracer/tracer.ts`, `graph_queries.ts`, `compiler/primitive/stdlib.ts` |
| A2 Card semantic state (contributions, contradiction, convergence policy) | `card_api.ts`, `runtime.ts`, card interface, PremisesSource; contribution identity. |
| Design: kernel, card construct, principles | `lain-viz/docs/DESIGN-CORE-03.md`, `design-principles.md` |

---

*Last updated: 2026-03-13. A2 = Card semantic state: contributions, contradiction, and convergence policy (expose contradiction/ambiguity; multiple candidates; local/global policy; optionally all candidates; card as compound propagator; connect/detach → build new, dispose old; preserve contribution identity for demo). A2 is now more important than generic hot-reload elegance. Re-prioritize as needed.*
