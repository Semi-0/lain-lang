/**
 * @deprecated Import from `../card_api.js` or `grpc/card` instead. This file re-exports the same
 * symbols as the canonical card API (metadata-backed lifecycle in `card_lifecycle.ts`).
 */
export * from "../card_api.js";
